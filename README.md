# images2gv

<br>
<div align="center">
<img src="https://img.shields.io/badge/LANGAGE-Go-blue" /> 
<img src="https://img.shields.io/badge/LICENSE-GPL-red" /><br>
</div>
<br>


`images2gv` is a CLI tool written in Go that converts a sequence of images into a `.gv` file using the **Extreme GPU Friendly Video Format**.

The `.gv` format is designed for ultra-fast, low-overhead video playback in creative coding environments and game engines. It achieves this by storing frames as individual LZ4-compressed texture blocks with a seekable index table at the end of the file.

## Features

- **Blazing Fast:** Uses a parallel worker pool to process and compress multiple frames concurrently.
- **GPU Friendly:** Produces files compatible with `ofxExtremeGpuVideo` and `ebiten_gvvideo`.
- **Low Memory Footprint:** Processes frames one by one (stream-like) rather than loading the entire sequence into RAM.
- **Flexible Input:** Supports PNG, JPEG, and GIF sequences.
- **Seekable:** Automatically generates the frame index table at the end of the file for $O(1)$ random access seeking.

## Installation

### Download Binaries

Pre-built binaries for Windows, macOS, and Linux (amd64 and arm64) are available on the [Releases page](https://github.com/chrplr/images2gv/releases).

### Install with Go

Ensure you have [Go](https://go.dev/doc/install) installed, then run:

```bash
go install github.com/chrplr/images2gv/cmd/images2gv@latest
```

## Usage

```bash
images2gv [options] <input_directory> <output.gv>
```

### Options

- `--fps`: Set the playback speed (default: `60.0`).
- `--force-size`: Crop/pad frames that do not match the first frame, instead of
  failing (see [Frame sizes](#frame-sizes)).

### Frame sizes

All frames must have the same dimensions. Sizes are checked up front, before
the output file is created, and a mismatch aborts with every offending file
listed:

```
3 of 8 frames do not match the first frame (64x48):
  frame_3.png is 100x100
  frame_5.png is 64x50
  frame_7.png is 32x24
All frames must be the same size. Pass --force-size to crop/pad them against the first frame instead.
```

Pass `--force-size` to get the permissive behaviour: every frame is drawn into
the first frame's dimensions, cropping what overflows and leaving transparent
padding where it falls short.

### Frame ordering

Frames are ordered by **natural numeric order**, so `frame_2.png` comes before
`frame_10.png`. A plain lexicographic sort would put `frame_10` first and
silently produce a video whose frames play out of sequence — with no error and
nothing to indicate anything went wrong.

Zero-padded names (`frame_0001.png`, as produced by `video2pngs.sh`) sort
correctly either way. When the natural and lexicographic orders differ,
`images2gv` prints a note so you can see that it mattered:

```
Note: using natural numeric order; a plain string sort would order these differently.
      First frame: frame_1.png
```

### Example

Convert a sequence of PNGs in the `frames/` folder to `video.gv` at 24 frames per second:

```bash
images2gv --fps 24 ./frames ./video.gv
```

## Technical Specification

The `.gv` file structure generated by this tool follows the binary specification:

1. **Header (24 bytes):**
   - `Width` (uint32)
   - `Height` (uint32)
   - `FrameCount` (uint32)
   - `FPS` (float32)
   - `Format` (uint32): Currently set to `0` (Raw RGBA).
   - `FrameBytes` (uint32): Uncompressed size of a single frame.

2. **Frame Data:**
   - Contiguous blocks of LZ4-compressed pixel data.

3. **Index Table (Footer):**
   - Located at the end of the file.
   - For each frame: `Address` (uint64) and `CompressedSize` (uint64).

## Dependencies

- [lz4/v4](https://github.com/pierrec/lz4): For high-speed frame compression.

## Additional tools in this projet:

* `video2pngs.sh`: extract frames from a video file (.mov, .mp4...) as a series of png images

* `cmd/player`:

   - [gvplayer](github.com/funatsufumiya/ebiten_gvvideo/gvplayer)
   - [ebiten](https://github.com/hajimehoshi/ebiten/v2)

## Acknowledgments

Based on the "Extreme GPU Friendly Video Format" originally implemented in C++ for OpenFrameworks ([ofxExtremeGpuVideo](https://github.com/Ushio/ofxExtremeGpuVideo)).

## License & Authorship

Author: Christophe Pallier <christophe@pallier.org>

[GPLv3](LICENSE)
